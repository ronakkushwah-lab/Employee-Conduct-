services:
  - type: web
    name: hrms-app
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn --pythonpath . dstt.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13

